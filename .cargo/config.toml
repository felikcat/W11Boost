[build]
target = "x86_64-pc-windows-msvc"

# Use LLD linker to speed up this part of compilation
[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"

[env]
# Tests must run serially - they share registry keys and can interfere
RUST_TEST_THREADS = "1"
